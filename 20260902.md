# 更新日志 - 2026-09-02

## 与上游 aquamarine5/ChaoxingSignFaker v1.17.1 同步

本次将核心签到链路与上游 Android 版 v1.17.1-stable-260826 对齐。

### 新增功能

- **滑块验证码闭环**：签到触发风控验证码时自动弹出验证码面板，拖动滑块校验通过后携带 `validate` 自动重新签到；支持刷新、跳过与本地验证码记忆（`captcha_memory`），滑块坐标归一化与上游一致（`x = value / 280 * 280 - 8`）
- **人脸识别签到**：活动要求人脸（`openCheckFaceFlag == 1`）时提供两种方式——自动获取账号默认人脸（下载后随机裁剪规避风控再上传）或从相册选择；签到前调用 `check-face-result` 换取 `faceEnc`，签到参数附带 `currentFaceId/ifCFP/courseId/faceCode/faceEncAid`；`signToken` 按上游算法（TreeMap 排序拼接 + clientId RSA 还原的 `sc`）计算
- **手势/签到码代签**：两种签到方式均支持为全部勾选用户代签，并接入验证码流程

### 优化改进

- **preSign 对齐上游**：改为 POST 并携带活动列表的 `data.ext` 表单参数，返回状态细分（无权限/已签到/可签到/已截止）；preSign 后回传 `pptSign/analysis` → `analysis2` 行为分析
- **签到结果统一解析**：完整覆盖上游全部响应状态（`success`/`success2`/`您已签到过了`/`validate`/`errorLocation_距离`/`[face]`/二维码过期/服务端错误），并据此展示
- **二维码签到**：经纬度对齐上游固定传 `-1`，自选位置以 `location` JSON（含 `mockData`）提交；修复地图选点不生效的问题；二维码过期时中断剩余代签用户
- **签到参数**：`uid` 改用与上游一致的 `puid`；`deviceCode` 按上游算法生成（sha256 双 UUID 后 Base64 88 字符）
- **代签间隔**：对齐上游 200ms/用户

### 依赖说明

- 群签到（Easemob IM）依赖上游未公开的 `imEncryptedKey`（构建时注入，仓库中不存在），本次未移植
- 赞助弹窗、签到统计上报、Stackbricks 更新等 Android 专属服务不在同步范围

**版本**：1.0.2 → 1.1.0
