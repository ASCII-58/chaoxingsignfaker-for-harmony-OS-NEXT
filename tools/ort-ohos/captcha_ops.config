# Required ops for the captcha YOLO11n fp16 model (opset 19; same source as
# rawfile/models/captcha_fp16_v2.onnx). Ops enumerated from the model:
# Add Cast Concat Conv Div MatMul MaxPool Mul Reshape Resize Sigmoid Slice Softmax Split Sub Transpose
# Format: <domain>;<opset>;<op1>,op2,... (opset import value; see docs/Reduced_Operator_Kernel_build.md)
# NOTE: keep this file ASCII-only - the ORT config parser uses the locale default encoding.
ai.onnx;19;Add,Cast,Concat,Conv,Div,MatMul,MaxPool,Mul,Reshape,Resize,Sigmoid,Slice,Softmax,Split,Sub,Transpose
